import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic-angular.app',
  appName: 'app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
